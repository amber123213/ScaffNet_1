# 文件路径: configs/scaffnet_pems04.yaml
base_dir: 'data/PEMS04' # 指向数据集文件夹
num_nodes: 307
input_dim: 1
output_dim: 1
rnn_units: 64
embed_dim: 10 # 脚手架图节点嵌入维度
seq_length: 12
horizon: 12
batch_size: 64
test_batch_size: 64
epochs: 200
patience: 20
learning_rate: 0.001
lambda_guidance: 0.01
lr_decay_rate: 0.7
lr_decay_step: 20 